# look for a table name dishes. Use singlular name. database create it in plural
# ActiveRecord convention is use plural as name in db and singular in class
# if not it will not work.
class Dish < ActiveRecord::Base
end